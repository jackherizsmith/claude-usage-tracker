{
  "name": "claude-usage-tracker",
  "version": "2.0.0",
  "description": "Track Claude Code session usage in macOS menu bar",
  "scripts": {
    "dev": "tauri dev",
    "build": "node scripts/create-icon.js && npx tauri icon build/icon.png && npx tauri build",
    "start": "tauri dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
